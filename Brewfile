# -*- mode: ruby -*-

# To use, first install Homebrew <https://brew.sh/>, then:
#
#     brew tap Homebrew/bundle
#     brew bundle
#
tap 'seanfisk/dmgbuild'
brew 'dmgbuild'
# Pandoc also has a Homebrew formula. There isn't a particular reason to pick
# one over the other.
cask 'pandoc'
